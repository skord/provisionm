<table class='table'>
	<thead>
	  <tr>
	    <th>Name</th>
	    <th></th>
	    <th></th>
	    <th></th>
	  </tr>
	</thead>
	<tbody>
		<% @buildings.each do |building| %>
		  <tr>
		    <td><%= building.name %></td>
		    <td><%= link_to 'Show', building, :class => 'btn' %></td>
		    <td><%= link_to 'Edit', edit_building_path(building), :class => 'btn btn-primary admin' %></td>
		    <td><%= link_to 'Destroy', building, confirm: 'Are you sure?', method: :delete, :class => 'btn btn-danger admin' %></td>
		  </tr>
		<% end %>
	</tbody>
</table>

<br />

<%= link_to 'New Location', new_building_path, :class => 'btn btn-success admin' %>
