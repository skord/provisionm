<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
 
      <!-- .btn-navbar is used as the toggle for collapsed navbar content -->
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
 
      <!-- Be sure to leave the brand out there if you want it shown -->
      <a class="brand" href="/">ProvisionM</a>
 
      <!-- Everything you want hidden at 940px or less, place within here -->
      <div class="nav-collapse">
        <ul class='nav'>
					<% if ['buildings'].include?(controller.controller_name)%>
						<li class='active'><%= link_to 'Where', buildings_path %></li>					
					<% else %>
						<li><%= link_to 'Where', buildings_path %></li>
					<% end %>					
					<% if ['manufacturers','vendors'].include?(controller.controller_name) %>
						<li class='active'><%= link_to 'Who', manufacturers_path %></li>
					<% else %>
	        	<li><%= link_to 'Who', manufacturers_path %></li>
					<% end %>
					<% if ['nodes','requisitions'].include?(controller.controller_name) %>
						<li class='active'><%= link_to 'What', nodes_path %></li>
					<% else %>
						<li><%= link_to 'What', nodes_path %></li>
					<% end %>
					<% if ['node_categories','services'].include?(controller.controller_name) %>
						<li class='active'><%= link_to 'Details', node_categories_path %></li>
					<% else %>
						<li><%= link_to 'Details', node_categories_path %></li>
					<% end %>
        </ul>
				<ul class='nav pull-right'>
					<% if controller.controller_name == 'help' %>
						<li class='active'><%= link_to 'Help', help_path %></li>
					<% else %>
						<li><%= link_to 'Help', help_path %></li>
					<% end %>
					<li class="divider-vertical"></li>					
					<li class="dropdown">
				    <a href="#"
				          class="dropdown-toggle"
				          data-toggle="dropdown">
				          Account
				          <b class="caret"></b>
				    </a>
				    <ul class="dropdown-menu">
				      <% if current_user %>
								<li><%= link_to 'Sign Out', destroy_user_session_path, method: :delete %></li>
							<% else %>
								<li><%= link_to 'Sign In', new_user_session_path %></li>
							<% end %>
				    </ul>
				  </li>
				</ul>
      </div>
    </div>
  </div>
</div>